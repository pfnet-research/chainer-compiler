#pragma once

#include <string>
#include <vector>

namespace chainer_compiler {
namespace runtime {

void TrainImagenet(const std::vector<std::string>& argv);

}  // namespace runtime
}  // namespace chainer_compiler
